# Scripts

Put project scripts here or use a `src/` directory in the project root.

Recommended scripts:

- `prepare_data.py`
- `train.py`
- `evaluate.py`
- `predict.py`
- `export_local.py`

Each script should accept a config file and write outputs to documented paths.

