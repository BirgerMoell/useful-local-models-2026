# 05 Context Extension

Hello-world context-length adaptation with RoPE scaling.

Run:

```bash
python 05_context_extension/train.py --steps 120
```

Teaches:

- short-context training;
- longer-context adaptation;
- RoPE scale as a context-extension knob;
- why long-context claims need evaluation.

