# 02 Instruction Tuning

Hello-world supervised fine-tuning on prompt/answer examples.

Run:

```bash
python 02_instruction_tuning/train.py --steps 120
```

Teaches:

- task formatting;
- SFT as next-token prediction over demonstrations;
- simple held-out probes;
- local checkpoint generation.

