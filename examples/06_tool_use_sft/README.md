# 06 Tool-Use SFT

Hello-world supervised fine-tuning for structured tool calls.

Run:

```bash
python 06_tool_use_sft/train.py --steps 120
```

Teaches:

- tool-call formatting;
- schema-like output;
- local task-specific behavior;
- why tool validity is an evaluation metric.

