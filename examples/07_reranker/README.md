# 07 Reranker

Hello-world local reranker training.

Run:

```bash
python 07_reranker/train.py --steps 120
```

Teaches:

- query/document relevance pairs;
- binary relevance training;
- local retrieval components;
- why not every project needs a generative LM.

